#include <stdio.h>
#include <string.h>

int main()
{
    char c[30];
    // char c1[30];
    char string1[] = "yassine";
    // char string2[]= "hanach";
    // printf("Entrer une chaine de caractere :");
    // scanf("%[^\n]",c);
    // printf("%s",strrev(c));
    // printf(strlwr(c));
    // printf(strupr(c));
    // printf(strcat(c,c1));
    // printf(strcpy(string2,string1));
    // printf(strset(string1,'M'));
    // printf(strlen(string1));
}